<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zl.easybuy.mapper.ProductCategoryMapper">

    <select id="queryProductCategoryById" parameterType="int" resultType="ProductCategory">
      SELECT id,name,parentId,type,iconClass  FROM easybuy_product_category WHERE  id = #{id}
   </select>

    <select id="queryProductCategorylist" parameterType="ProductCategoryBO" resultType="ProductCategory">
        SELECT epc1.*,epc2.name as parentName FROM easybuy_product_category epc1 LEFT JOIN easybuy_product_category epc2
        ON epc1.parentId=epc2.id
        <where>
            <if test="condition != null">
                <if test="condition.productCategory != null">
                    <if test="condition.productCategory.name != null and condition.productCategory.name!=''">
                        and epc1.name like '%${condition.productCategory.name}%'
                    </if>
                    <if test="condition.productCategory.parentId!=null">
                        and epc1.parentId = #{condition.productCategory.parentId}
                    </if>
                    <if test="condition.productCategory.type != null">
                        and epc1.type = #{condition.productCategory.type}
                    </if>
                </if>
            </if>
        </where>
        <if test="condition.isPage">
            limit #{condition.startIndex} , #{condition.pageSize}
        </if>
    </select>
</mapper>